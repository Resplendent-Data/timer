interface:
  display_name: "Resplendent Release"
  short_description: "Ship versioned releases for this repository"
  default_prompt: "Use $resplendent-release to prepare and ship a release: sync version files, run checks, commit, push master, and verify the release workflow."
